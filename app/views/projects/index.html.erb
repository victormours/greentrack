Current projects:<br>
<% @projects.each do |project| %>
    <%= project.name %>
<% end %>


<%= form_for Project.new, :url => { :action => "create" } do |f| %>
  <%= f.text_field :name %>
  <%= f.submit "Create" %>
<% end %>